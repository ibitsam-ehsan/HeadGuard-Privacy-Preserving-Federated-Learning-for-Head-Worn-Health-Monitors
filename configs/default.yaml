privacy:
  epsilon: 4.0
  delta: 1e-5
  clip_norm: 1.0

data:
  sampling_rate: 64
  window_seconds: 10

training:
  epochs: 10
  batch_size: 32
  learning_rate: 0.001

model:
  conv_channels: [32, 64, 128]
  kernel_sizes: [5, 5, 3]
